frontend folder of project is "typito", and backend is in the "backend" folder
to run frontend
    cd typito
    npm i (to install dependencies)
    npm run dev

to run backend
    cd backend
    npm i (to install dependencies)
    npm start